---
import HTML from "../icons/HTML.astro"
import NodeJS from "../icons/Node.astro"
import CSS from "../icons/CSS.astro"
import JAVASCRIPT from "../icons/JavaScript.astro"

const TAGS = {
    NODE:{
        name: 'NODE', 
        class: 'bg-black text-white', 
        icon: NodeJS,
    },
    HTML:{
        name: 'HTML',
        class: 'bg-white text-[#73bcf6]',
        icon: HTML,
    },
    CSS:{
        name: 'CSS',
        class: 'bg-black text-white',
        icon: CSS,     
    },
    JAVASCRIPT:{
        name: 'JAVASCRIPT',
        class: 'bg-black text-white',
        icon: JAVASCRIPT,     
    }
}

const PROJECTS = [
    {
        title: 'Interactive Weather App',
        description: 
        "The Interactive Weather App is a web application that allows users to check the weather of various cities in real-time. The project consists of a frontend developed in HTML, CSS, and JavaScript, and a backend built with Node.js and Express.",
        github: 'https://github.com/Llarg94/App-clima',
        image: '/projects/appClima.png',
        tags: [TAGS.NODE, TAGS.HTML, TAGS.CSS, TAGS.JAVASCRIPT]
    }
]
---

{
    PROJECTS.map(({ image, title, description, tags})  => (
        <article>
            <h3 class="text-2xl font-semibold text-yellow-200 mb-2 ">{title}</h3>
            <p class="text-lg mb-4 text-pretty">{description}</p>
            <ul class="flex gap-x-2 flex-row">
                {tags.map((tag) => (
                    <li>
                        <span class={`flex gap-x-2 rounded-full text-xs ${tag.class} py-1 px-2 mb-4`}>
                            <tag.icon class="size-4"/>
                            {tag.name}
                        </span>
                    </li>
                ))}
            </ul>
            <img 
            class="rounded shadow-2xl shadow-white/10" 
            src={image} 
            alt={`Captura de pantalla del proyecto ${image}`}/>
        </article>
    ))
}
